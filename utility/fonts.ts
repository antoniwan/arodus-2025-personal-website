import { Newsreader, Red_Hat_Text } from "next/font/google";

export const textFont = Red_Hat_Text({
  subsets: ["latin"],
  display: "swap",
});

export const headingFont = Newsreader({
  subsets: ["latin"],
  display: "swap",
});
